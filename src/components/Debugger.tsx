import React, { FC } from 'react';
// import { todoArray } from '../api/todos.api';

export const Debugger: FC = () => {
  return <ul>
    {/*{todoArray.map(todo => <li key={todo.id}>{todo.name}</li>)}*/}
  </ul>;
};
